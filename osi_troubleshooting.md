# مدل OSI و عیب‌یابی (Troubleshooting) دسترسی به وبسایت

## مقدمه
مدل OSI (Open Systems Interconnection) یک مدل مرجع است که ارتباطات شبکه را به ۷ لایه مجزا تقسیم می‌کند. هر لایه وظیفه مشخصی دارد و برای عیب‌یابی شبکه، معمولاً از لایه ۱ (فیزیکی) به سمت لایه ۷ (کاربردی) حرکت می‌کنیم تا مشکل را ریشه‌یابی کنیم.

---

## لایه ۱: فیزیکی (Physical)
**وظیفه:** انتقال بیت‌های خام داده از طریق رسانه فیزیکی (کابل، فیبر نوری، امواج رادیویی).
**تشریح:** این لایه با مشخصات سخت‌افزاری مانند ولتاژ، نرخ داده، کانکتورها و کابل‌ها سروکار دارد.

**بررسی در سناریوی "عدم دسترسی به وبسایت":**
- آیا کابل شبکه به درستی وصل است؟
- آیا چراغ‌های کارت شبکه (NIC) روشن هستند؟
- آیا اتصال Wi-Fi برقرار است و سیگنال کافی دارد؟

**ابزار/دستور لینوکس:**
- `ethtool ens33` → وضعیت کابل و سرعت اتصال را نشان می‌دهد.
- `ip link` → وضعیت اینترفیس‌های شبکه را نمایش می‌دهد (UP/DOWN).

---

## لایه ۲: پیوند داده (Data Link)
**وظیفه:** انتقال فریم (Frame) بین دو گره متصل در شبکه محلی، کنترل خطا و دسترسی به رسانه.
**تشریح:** این لایه آدرس‌های MAC را مدیریت کرده و داده را به صورت فریم های قابل تشخیص برای لایه فیزیکی بسته‌بندی می‌کند.

**بررسی در سناریوی "عدم دسترسی به وبسایت":**
- آیا جدول ARP (Address Resolution Protocol) کامل است؟ آیا آدرس MAC مقصد شناسایی شده است؟
- آیا در شبکه محلی تداخل یا مشکل switch وجود دارد؟

**ابزار/دستور لینوکس:**
- `ip neigh` → نمایش جدول همسایگان (ARP cache).
- `arp -n` → مشاهده نگاشت IP به MAC.

---

## لایه ۳: شبکه (Network)
**وظیفه:** مسیریابی (Routing) بسته‌ها از مبدأ به مقصد، تعیین بهترین مسیر و مدیریت آدرس‌دهی منطقی (IP).
**تشریح:** این لایه بسته‌ها را با استفاده از آدرس‌های IP در شبکه‌های مختلف هدایت می‌کند.

**بررسی در سناریوی "عدم دسترسی به وبسایت":**
- آیا سیستم شما IP معتبر دارد؟
- آیا Gateway پیش‌فرض در دسترس است؟
- آیا مسیر رسیدن به سرور مقصد (مثلاً 8.8.8.8) وجود دارد؟

**ابزار/دستور لینوکس:**
- `ip addr` → نمایش IP اختصاص داده شده به اینترفیس.
- `ping 8.8.8.8` → تست اتصال به یک سرور خارجی (بررسی دسترسی به اینترنت).
- `traceroute google.com` → مشاهده مسیر پیموده شده تا مقصد.

---

## لایه ۴: حمل و نقل (Transport)
**وظیفه:** ایجاد ارتباط End-to-End، تقسیم داده به بخش‌های کوچکتر (Segment)، کنترل جریان و تضمین تحویل داده (TCP) یا عدم تضمین (UDP).
**تشریح:** پورت‌ها (مثلاً 80 برای HTTP، 443 برای HTTPS) در این لایه تعریف می‌شوند.

**بررسی در سناریوی "عدم دسترسی به وبسایت":**
- آیا پورت ۸۰ (HTTP) یا ۴۴۳ (HTTPS) روی سرور مقصد باز است؟
- آیا فایروال این پورت‌ها را مسدود کرده است؟

**ابزار/دستور لینوکس:**
- `ss -tln` → نمایش پورت‌های TCP در حال LISTEN.
- `telnet google.com 80` → تست دسترسی به پورت ۸۰ یک سرور.
- `nc -zv google.com 443` → تست پورت ۴۴۳ با netcat.

---

## لایه ۵: جلسه (Session)
**وظیفه:** برقراری، مدیریت و پایان دادن به جلسات ارتباطی بین دو برنامه کاربردی  **تشریح:** این لایه مکالمه (Dialog) را مدیریت کرده و نقاط بازگشت (Checkpoint) را برای ادامه ارتباط پس از قطعی فراهم می‌کند.

**بررسی در سناریوی "عدم دسترسی به وبسایت":**
- آیا ارتباط TCP به درستی برقرار شده  (3way handshake)؟
- آیا جلسه به دلیل timeout یا خطا قطع شده است؟

**ابزار/دستور لینوکس:**
- `ss -tnp` → نمایش اتصالات TCP فعال و وضعیت آن‌ها.
- `lsof -i :80` → مشاهده برنامه‌هایی که از پورت ۸۰ استفاده می‌کنند.

---

## لایه ۶: نمایش (Presentation)
**وظیفه:** ترجمه داده‌ها، رمزگذاری، فشرده‌سازی و تبدیل فرمت‌ها (مانند ASCII به EBCDIC).
**تشریح:** این لایه تضمین می‌کند که داده ارسال شده از لایه کاربرد، توسط سیستم مقصد قابل خواندن باشد.

**بررسی در سناریوی "عدم دسترسی به وبسایت":**
- آیا رمزگذاری SSL/TLS به درستی انجام می‌شود (برای وبسایت‌های HTTPS)؟
- آیا وبسایت گواهی SSL معتبر دارد؟

**ابزار/دستور لینوکس:**
- `openssl s_client -connect google.com:443` → بررسی جزئیات گواهی SSL و ارتباط امن.
- `echo | openssl s_client -servername google.com -connect google.com:443 2>/dev/null | openssl x509 -noout -dates` → نمایش تاریخ انقضای گواهی.

---

## لایه ۷: کاربرد (Application)
**وظیفه:** تعامل مستقیم با کاربر و ارائه خدمات شبکه (مانند HTTP، FTP، DNS، SMTP).
**تشریح:** این لایه شامل پروتکل‌هایی است که برنامه‌های کاربردی برای تبادل داده از آن‌ها استفاده می‌کنند.

**بررسی در سناریوی "عدم دسترسی به وبسایت":**
- آیا نام دامنه (Domain) به درستی به IP تبدیل می‌شود (DNS)؟
- آیا سرور پاسخ HTTP مناسبی برمی‌گرداند (کدهای ۲۰۰، ۴۰۴، ۵۰۰)؟

**ابزار/دستور لینوکس:**
- `nslookup google.com` → بررسی تبدیل نام دامنه به IP.
- `curl -I google.com` → نمایش هدرهای HTTP پاسخ سرور.
- `wget -S google.com` → دریافت پاسخ سرور با جزئیات.
